#include<stdio.h>
int main() {
	int a[100],k;
	scanf("%d", &k);
	for (int i = 0; i < k; i++) {
		scanf("%d", &a[i]);
	}
	for (int c = 0; c < 2; c++) {
		for (int i = 0; i < k; i++) {
			printf("%d\n", a[i]);
		}
	}
	return 0;
}
