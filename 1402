#include<stdio.h>
int main() {
	int a;
	scanf("%d", &a);
	int n[1000];
	for (int i = 0; i < a; i++) {
		scanf("%d", &n[i]);
	}
	a--;
	for (int i = 0; a >= i; a--) {
		printf("%d ", n[a]);
	}
	return 0;
}
